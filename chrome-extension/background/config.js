/**
 * Chrome extension background config
 *
 * Shared constants used by the service worker helpers. Kept in its
 * own module so swapping AEM environments only requires editing one
 * file.
 */
export const AEM_HOST_PREFIX = 'https://author-p125318-e1369672.adobeaemcloud.com'
